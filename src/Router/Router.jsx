import { createBrowserRouter } from "react-router-dom";

const myCreateRoute = createBrowserRouter([
    {
        path: '/',
        element: <div> this is for testening</div>
    }
])

export default myCreateRoute